package testing;


import util.Utilities;

import java.util.List;
import java.util.stream.Collectors;
import java.util.stream.IntStream;

public class UtilitiesTest {

    public static void main(String[] args) {
//        List<Integer> first_list = IntStream.range(0, 5).boxed().collect(Collectors.toList());
//        List<Integer> second_list = IntStream.range(7, 9).boxed().collect(Collectors.toList());
//
//        var all_subsets = Utilities.generate_all_subsets(first_list);
//        System.out.printf("Possible Subsets of the list [%s] (%d in total)\n", first_list.stream().map(String::valueOf).collect(Collectors.joining(", ")), all_subsets.size());
//        for (var subset : all_subsets) {
//            System.out.printf("[%s]\n", subset.stream().map(String::valueOf).collect(Collectors.joining(", ")));
//        }
//
//        var all_merges = Utilities.generate_all_merges(first_list.size(), second_list.size());
//        System.out.printf("Possible Merges of the lists of size %d and %d (%d in total)\n", first_list.size(), second_list.size(), all_merges.size());
//        for (var merge : all_merges) {
//            System.out.printf("[%s]\n", merge.stream().map(String::valueOf).collect(Collectors.joining(", ")));
//        }

    }
}
